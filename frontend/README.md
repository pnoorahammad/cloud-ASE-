# Frontend

This is a minimal React + TypeScript frontend for the Salesforce Validation Rule Manager.

Start locally:

```bash
cd frontend
npm install
npm run dev
```

The frontend expects the backend API at `VITE_API_BASE` (defaults to `http://localhost:5000`).
